# Optional: Media3 FFmpeg Extension
To enable hardware-accelerated fallback rendering via Media3,
copy lib-decoder-ffmpeg-release.aar here from the Just Player (exobase) project.
It was built with: truehd, mlp, dca, eac3, ac3, flac, opus decoders.
